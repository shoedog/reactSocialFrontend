import React from 'react';
import style from './stream.css';

const StreamContent = (props) => {
	return(
		<div>
			<h2>This is the Stream Area</h2>
			<p>Fill with input from social feed</p>
		</div>
	);
}


export default StreamContent;